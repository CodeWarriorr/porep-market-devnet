diff --git a/cmd/sptool/toolbox_deal_client.go b/cmd/sptool/toolbox_deal_client.go
index 550d749c..6768cfa7 100644
--- a/cmd/sptool/toolbox_deal_client.go
+++ b/cmd/sptool/toolbox_deal_client.go
@@ -1681,6 +1681,14 @@ var mk20DealCmd = &cli.Command{
 			Name:  "market-deal-id",
 			Usage: "market deal ID",
 		},
+		&cli.StringFlag{
+			Name:  "notification-address",
+			Usage: "Filecoin address receiving the FIP-0109 notification",
+		},
+		&cli.StringFlag{
+			Name:  "notification-payload",
+			Usage: "hex-encoded FIP-0109 receiver payload",
+		},
 		&cli.Uint64Flag{
 			Name:  "allocation",
 			Usage: "allocation id of the deal",
@@ -1925,6 +1933,24 @@ var mk20DealCmd = &cli.Command{
 			}
 		}
 
+		if cctx.IsSet("notification-address") {
+			if !cctx.IsSet("notification-payload") {
+				return xerrors.Errorf("notification payload is required with notification address")
+			}
+			notificationAddress, err := address.NewFromString(cctx.String("notification-address"))
+			if err != nil {
+				return xerrors.Errorf("parsing notification address: %w", err)
+			}
+			notificationPayload, err := hex.DecodeString(strings.TrimPrefix(cctx.String("notification-payload"), "0x"))
+			if err != nil {
+				return xerrors.Errorf("decoding notification payload: %w", err)
+			}
+			p.DDOV1.NotificationAddress = notificationAddress
+			p.DDOV1.NotificationPayload = notificationPayload
+		} else if cctx.IsSet("notification-payload") {
+			return xerrors.Errorf("notification address is required with notification payload")
+		}
+
 		if cctx.Uint64("allocation") != 0 {
 			p.DDOV1.AllocationId = new(verifreg.AllocationId(cctx.Uint64("allocation")))
 		}
diff --git a/tasks/piece/task_park_piece.go b/tasks/piece/task_park_piece.go
--- a/tasks/piece/task_park_piece.go
+++ b/tasks/piece/task_park_piece.go
@@ -265,7 +265,7 @@ func (p *ParkPieceTask) CanAccept(ids []harmonytask.TaskID, engine *harmonytask
 }
 
 func (p *ParkPieceTask) TypeDetails() harmonytask.TaskTypeDetails {
-	const maxSizePiece = 64 << 30
+	const maxSizePiece = 8 << 20
 
 	taskName := tasknames.ParkPiece
 	if p.longTerm {
diff --git a/market/mk20/ddo_v1.go b/market/mk20/ddo_v1.go
--- a/market/mk20/ddo_v1.go
+++ b/market/mk20/ddo_v1.go
@@ -127,10 +127,6 @@ func (d *DDOV1) VerifyMarketDeal(ctx context.Context, db *harmonydb.DB, eth ethc
 		return Ok, nil
 	}
 
-	if d.MarketDealID == nil {
-		return ErrProductValidationFailed, xerrors.Errorf("market deal id is not set")
-	}
-
 	if deal == nil {
 		return ErrBadProposal, xerrors.Errorf("deal is nil")
 	}
@@ -153,6 +149,10 @@ func (d *DDOV1) VerifyMarketDeal(ctx context.Context, db *harmonydb.DB, eth ethc
 		return ErrBadProposal, xerrors.Errorf("market contract is not allowed by storage provider")
 	}
 
+	if d.MarketDealID == nil {
+		return Ok, nil
+	}
+
 	market, err := mk20contract.NewCurioDealViewV1Caller(common.HexToAddress(d.MarketAddress), eth)
 	if err != nil {
 		return ErrServerInternalError, xerrors.Errorf("creating CurioDealViewV1 caller: %w", err)
